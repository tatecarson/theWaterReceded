var oscMin = require('osc-min')
  , original = 2147483643
  , data, decoded

data = oscMin.toBuffer({ address: '/', args: [original] })
decoded = oscMin.fromBuffer(data)
console.log(original)
console.log(decoded.args[0].value)